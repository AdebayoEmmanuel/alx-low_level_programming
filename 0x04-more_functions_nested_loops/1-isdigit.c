#include "main.h"
/**
* _isupper - checks if a character is digit 0-9 or not.
* @c: character to be checked
* Return: 1 if digit 0-9, 0 otherwise.
*
*/
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
