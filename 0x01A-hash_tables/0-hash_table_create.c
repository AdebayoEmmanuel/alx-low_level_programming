#include "hash_tables.h"

/**
 * hash_table_create - creates a new hash table
 * @size: size of hash table array
 *
 * Return: ponter to new hash table
 * returns NULL on failure
 */
hash_table_t *hash_table_create(unsigned long int size)
{
	hash_table *ht;	
}
